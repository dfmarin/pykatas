from typing import Dict, List


def get_total(costs: Dict[str, float], items: List[str], tax: float) -> float:
    """
    Calculate total cost including tax.

    Parameters:
    costs (dict): Mapping from item name to price.
    items (list): List of purchased item names.
    tax (float): Tax rate as a decimal (e.g., 0.09 for 9%).

    Returns:
    float: Total cost rounded to two decimal places.
    """
    pass
